Resuming EnergyLink FLEX. First read the cumulative handoff at
/Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-06-v3.md and the project memory (MEMORY.md
auto-loads; start with live_qa_session4.md, then project_status.md, package_b_parked.md). Trust
them — don't re-derive.

One-line context: session 4 was live browser QA. We SHIPPED (uncommitted) a layout redesign
(component sidebar → left drawer with the NL bar embedded; layers panel deleted; zoom bottom-right),
bug #3 stretch-corruption GUARDS (text held rigid + gross-distortion >2.5x refused with a warning
instead of ovaling everything), and bug #2 overlay alignment (overlays now derived from real
dimension-block geometry so they sit on the equipment; bonus: stopped a latent bug that was
clobbering the AI's real dim values). Bug #1 (black blobs) is BLOCKED at the post-render layer —
they're solid-hatch/nozzle symbols with no selectable signature (no layer attr, no fill, no color);
the clean fix is parser-level (filter HATCH/SOLID or emit layer classes in dwg_to_svg()).

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint,
10 commits ahead of origin/wip, NOT pushed) — NEVER the Google Drive copy (FUSE won't boot the
dev server). Dev server = port 3002. Lab at ~/dev/elf-lab.

Git: HEAD = e971050; this session added NO commits — all work is uncommitted (6 modified + 1 new
test file src/lib/dwg/__tests__/svg-stretch.guards.test.ts; harmless untracked .npm-install.log).
src/auth.ts dev-login is REVERTED (clean tree). vitest 95/95, tsc clean, eslint 0 errors.

To run live: re-add the auth.ts dev-login shortcut (revert before any commit), start on 3002, log
in as "energylink" (any password), open /editor?project=215a08eb-403d-41c7-b0f9-0fd6cceecac2. The
44MB Sheet_2 SVG needs the dev-only {cache:'no-store'} fetch already in loadSvg (preview browser
can't cache it); load takes ~15-18s. Trigger stretches via the left-drawer dim inputs (the drawing
dim-click setup is flaky across reloads).

Guardrails: commit/push ONLY when I ask; never push main (prod deploy); revert the auth.ts dev-login
before any commit. No customer .dwg/.svg in the repo. Never overwrite a deliverable (version it).
BLUF, no em-dashes, decide-and-act on reversible work, one question at a time.

Confirm git state (git log --oneline -1 => e971050; git status shows the 6 M + guards test) and
npm test green (95/95), then ask me which direction I want before writing code. Likely candidates:
  1. Commit this session's work (revert dev-login first; never main; push only if I say).
  2. Bug #1 parser-level: investigate whether @mlightcad/libredwg-web can filter HATCH/SOLID
     entities or emit layer info in dwg_to_svg() (parser.ts). Bigger, outcome uncertain.
  3. Deep #3 scoping: hold neighbors rigid so sub-2.5x width edits stop ovaling (now more feasible
     since #2 gives real component geometry). Package-B-class — see package_b_parked memory.
  4. Production AI: add the Vercel Gateway card OR set ANTHROPIC_API_KEY in Vercel prod env.
  5. Mechanical parser known-defects (text escaping, nozzle labels, GA-vs-P&ID sheet-type detection).
