Resuming EnergyLink FLEX. First read the cumulative handoff at
/Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-07-v4.md and the project memory (MEMORY.md
auto-loads; start with package_b_parked.md — the primary "2D scoping" record — then project_status.md,
live_qa_session4.md, fable_operating_model.md). Trust them — don't re-derive.

One-line context: session 5 SHIPPED (committed, not pushed) two big things — (1) bug #1 black blobs
FIXED + live-verified (they were 17 CriticalFeature annotation <use> symbols; stripAnnotationUses()
strips them at ingestion), and (2) the whole Unified 2D Scoping engine (deep #3 + Package B) BUILT via
the full spec→2-review→2-spike→plan→implement pipeline with U2 farmed to Fable and Opus-verified. All 8
plan tasks done: U1 computeComponentBand (dim-geometry.ts), U2 crossBand scoping (svg-stretch.ts,
Fable-built), U3 conservative gap detector (detached.ts), U4 WARN + wiring (svg-drawing-canvas.tsx
applyAllStretches). vitest 116/116, tsc 0, eslint 0.

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint, HEAD
d5f02c2, 23 commits ahead of origin/wip, NOT pushed) — NEVER the Google Drive copy (FUSE won't boot the
dev server). Dev server = port 3002. Lab at ~/dev/elf-lab.

THE ONE UNFINISHED THING: the live end-to-end app edit for 2D scoping is NOT yet confirmed. The engine
is verified (crossband harness 6/6, real-Sheet_2 nested golden no-regression, U1/U3 unit tests), but a
browser-triggered width edit couldn't be scripted (React controlled dim-input won't commit via synthetic
input/Enter events — 0 transforms, no [ELF stretch] log; the dim-input flakiness the handoff warns about).
RECOMMENDED CLOSE: build an offline G1 golden in scripts/qa/scope-golden.mjs (real Sheet_2, width edit on
a both-axis-dimensioned component via applyMultiStretch with the U1 crossBand → assert a stacked neighbour
circle keeps its aspect ratio AND the target widens). Deterministic, no flaky UI.

Guardrails: commit/push ONLY when I ask; never push main (prod deploy); revert the auth.ts dev-login
before any commit (currently reverted, tree clean). No customer .dwg/.svg in the repo. Never overwrite a
deliverable (version it). Fable = build-time only, spend it on hard durable algorithms with a harness.
BLUF, no em-dashes, decide-and-act on reversible work, one question at a time.

Confirm git state (git log --oneline -1 => d5f02c2; git status clean; 23 ahead of origin/wip) and npm
test green (116/116), then ask me which direction I want before writing code. Likely candidates:
  1. Close the 2D-scoping live gap: build the offline G1 golden on real Sheet_2 (recommended), OR do a
     REAL manual browser width edit to eyeball that stacked neighbours stop ovaling.
  2. Push the branch to origin/wip to back up 23 commits of work (never main).
  3. Investigate the pre-existing nested-golden "resolved edit" silencer failure (parked
     derive/redistribute wiring; predates this work).
  4. Production AI: add the Vercel Gateway card OR set ANTHROPIC_API_KEY in Vercel prod env.
  5. Mechanical parser known-defects (text escaping, nozzle labels, GA-vs-P&ID sheet-type detection).
