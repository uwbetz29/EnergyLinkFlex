# EnergyLink FLEX — Cumulative Handoff (2026-07-07, session 7 / "v5")

> Supersedes `docs/HANDOFF-2026-07-07-v4.md`. Self-contained enough to resume cold.
> Project memory also auto-loads each session (`~/.claude/projects/…EnergyLinkFlex/memory/`,
> index `MEMORY.md` → `project_status.md` has the freshest SESSION 7 detail).

---

## BLUF

EnergyLink FLEX (ELF) is a **sales-configuration tool** for an engineering firm selling **SCR/CO
catalyst systems** (exhaust scrubbers for gas-turbine power plants; TITAN PGM 130 base design). A
salesperson opens a base CAD drawing (DWG→SVG via LibreDWG WASM), adapts section dimensions for a
customer bid, adds red markups, and exports. Speed + clarity over CAD precision, but multimillion-dollar
bids depend on correct output. **Not a CAD tool.**

**Session 7 shipped a LOT, all DEPLOYED TO PRODUCTION and live-verified:**
1. **P&ID handling + red markups + buttons-only zoom** (the 10-task feature from the plan) — merged to
   `main`, deployed. P&ID sheets show a "not resizable" badge + inert resize; red line/arrow/text markups
   persist (`dwg_markups` jsonb); zoom is buttons-only.
2. **White-overlay bug FIXED** (`5b87847`): the markup overlay `<svg>` was painting white over the whole
   drawing (inline-SVG `<style>` leak). Fixed with `background:transparent`, then properly with `.elf-dwg`
   CSS scoping in Phase A.
3. **Phase A hotfixes** (`79ffdbe` nl-bar, `65e5fcd` cluster) — 6.5 of Mike's 7 reported bugs, all
   live-verified on local dev then deployed. Details below.

**Prod is LIVE at https://energylinkflex.vercel.app** running commit `65e5fcd`.

---

## Stack / infra

Next.js 16 + Turbopack · Neon Postgres · Auth.js v5 · Vercel Blob · AI SDK v6 · LibreDWG WASM
(`@mlightcad/libredwg-web`). Repo `uwbetz29/EnergyLinkFlex`, Vercel project `mikebetz/energylinkflex`
(CLI-linked in the local clone, `vercel` authed as `uwbetz29`).
**AI model:** `THINKING_MODEL` in `src/lib/ai/scr-knowledge.ts` = Opus 4.8 (direct-Anthropic when
`ANTHROPIC_API_KEY` set, else the gateway slug). **Prod AI is gated** — gateway fallback unless
`ANTHROPIC_API_KEY` is added to the Vercel prod env (deterministic features work regardless; only the
AI cascade / NL-bar needs it).

---

## Git / deploy state

- **Work dir:** `/Users/mike/dev/EnergyLinkFlex` (LOCAL clone — develop here, NEVER the Google Drive copy;
  its FUSE mount won't boot the dev server). Dev server = **port 3002**.
- **Branch `wip/dwg-ai-stretch-checkpoint` = `main` = prod, all at `65e5fcd`.** Tree clean.
  `origin/main` in sync (0 ahead). `src/auth.ts` dev-login reverted.
- **Mike EXPLICITLY authorized merging to main + prod deploys this session** (one-time override of the
  standing "never push main" guardrail — the guardrail still holds by default for future sessions unless
  Mike says otherwise).
- **DB migrations already run against the shared Neon DB** (prod `DATABASE_URL` == the migrated host
  `ep-nameless-credit-anxyj9i9`): `dwg_sheet_type text`, `dwg_markups jsonb`.

---

## Login (prod + local)

- **Prod:** https://energylinkflex.vercel.app/login → username **`energylink`** / password **`Linder`**
  (I set that bcrypt hash on the `energylink`/ Mike Betz account this session; Mike may change it via
  `/auth/update-password`). That account owns the QA projects.
- **Test project (Sheet_2, GA):** `215a08eb-403d-41c7-b0f9-0fd6cceecac2`. **P&ID QA project:**
  `87be7ec3-e9a7-47d1-bdb1-d271a72fd2b9` ("P&ID QA 24189-0010").
- **Local dev QA:** re-add the DEV-ONLY `auth.ts` login shortcut (in Credentials `authorize`, before the
  null check: if `NODE_ENV!=="production"` and `credentials.email===DEV_LOGIN_EMAIL`, look up the
  `user_profiles` row and return it). **Revert before any commit.** `DEV_LOGIN_EMAIL=energylink` is in
  gitignored `.env.local`. Start via Claude preview MCP `preview_start "dev"` (launch.json "dev", port
  3002, cwd `local-elf`→ the clone). The 44MB Sheet_2 SVG loads ~15s.
- **VERIFY LESSON (important):** **preview deploys FAIL auth** — Vercel scopes the DB/auth env vars to
  the *Production* environment only, so a `vercel` (preview) deployment can't reach Neon and login bounces.
  Verify on **local dev** (dev-login) or **prod**, NOT a preview URL.

---

## Phase A — what shipped (all live-verified on local dev, then deployed)

| Fix | What | Where |
|---|---|---|
| A1 | `dampenHatch` (Fable-built): gray out short (<0.35u) hatch `<line>` segments so exploded HATCH stops flooding black at low zoom ("black blobs"). 31k groups grayed; **big blobs gone**. | `src/lib/dwg/hatch.ts` + harness `__tests__/hatch.test.ts`; wired at `svg-drawing-canvas.tsx` `processLibreDwgSvg` (`dampenHatch(stripAnnotationUses(svgText))…`). |
| A2 | Markup select/delete: component boxes → `pointerEvents:none` when a markup tool is active; overlay `z:10` when active; zoom-invariant hit tolerance. | `svg-drawing-canvas.tsx` (box style ~L1626, `markupTool` destructure), `markup-overlay.tsx` (z-index, hit-tol). |
| A3 | Overlapping components: `selectComponentAtPoint` = smallest-enclosing box first, click-to-cycle outward. | `svg-drawing-canvas.tsx` (`compOverlayRef`, resolver, box `onClick`). |
| A3-scope | Scope the drawing's injected `<style>` to `.elf-dwg` (inline-SVG `<style>` leaks document-wide → was rendering markup text BLACK + arrowheads HOLLOW). Now arrowheads fill red `rgb(225,29,42)`. | `svg-drawing-canvas.tsx` `processLibreDwgSvg` style block. |
| A4 | Return to **Pan** after drawing a markup (overlay was eating all clicks → dimension/component select died). | `markup-overlay.tsx` (3 `setMarkupTool("pan")` sites). |
| A5 | Dimension-edit popup was STUCK (focus stayed on `<body>`, no global Esc, no click-outside). Now: reliable autofocus + global Escape + click-outside + ✕ button. | `svg-drawing-canvas.tsx` `DimensionEditor`. |
| A6 | NL bar → **"Ask FLEX AI"** panel: heading + pitch, response area (scroll-capped, success/caution/critical hierarchy), example chips, multiline composer + gradient Apply. Stays in the left drawer (Mike's call). | `src/components/editor/nl-bar.tsx`. |

vitest **139/139**, tsc 0, eslint 0 errors. Verified live (energylink/Linder): blobs gray, markup select+delete, component cycle, arrowhead red, return-to-pan, popup dismiss (Esc + click-outside), NL bar.

---

## OPEN WORK (next session, in order)

### 1. Blob v2 (small, do first)
A1's `dampenHatch` is **`<line>`-only** and misses dense **`<path>`** and **`<circle>`/`<ellipse>`**
equipment clusters → a few *small* residual black blobs remain on prod. Extend `dampenHatch` (same
ingestion-string pass, keep the Fable-harness discipline) to also gray:
- short `<path>` subsegments (parse `d`, or bound the path's extent), and
- small `<circle r>` / `<ellipse rx ry>`.
Add cases to `__tests__/hatch.test.ts` first (RED), farm the impl to Fable or extend inline, verify LIVE
on local dev (the residual blobs visibly gone on Sheet_2 equipment tops), then deploy.

### 2. Phase B — restore the dead basics (the real trust-killers, per the plan)
Plan doc: **`docs/superpowers/plans/2026-07-07-fixes-and-ux-plan.md`** (Kano/Chisnell tiered; read it).
The 5-agent investigation (`elf-investigate-solutionize` workflow) found three basics silently dead +
more. Ordered:
- **B1 Wire Undo/Redo** — store `undo()`/`redo()` already exist (`editor-store.ts`); the top-bar Undo
  button (`editor-shell.tsx:349`) isn't wired. Add + Cmd/Ctrl+Z. (Pure wiring, S.)
- **B2 Persist dimension edits** — `updateDim` only mutates Zustand; nothing saves to the server. Mirror
  the markup debounce PATCH pattern (`editor-shell.tsx`). (M.)
- **B3 Wire Export** — the Export button (`editor-shell.tsx:353`) has NO `onClick`; nothing serializes.
  Serialize the live drawing (stretches + markups) → **PDF + PNG** (Mike's choice), PDF stamped with the
  title block from `dwgMetadata` (drawing #, customer, scale). (L.)
- **B4 Save-state indicator** (Saving…/Saved). (S.)
- **B5 Markups follow a stretch** — overlay viewBox is frozen at load; a stretch grows the drawing
  viewBox so markups drift off-target (correctness risk). Re-sync overlay viewBox after
  `applyAllStretches`, or remap markups. (Fable candidate: coord remap + harness.)
- **B6 Multi-sheet blank fix** — `setSheets` never calls `setActiveSheet(0)`/sets `svgUrl` (stale
  comment) → multi-sheet DWGs load blank + first-sheet markups scope to the wrong number.
  (`editor-store.ts`.)

### Also-found (Tier 1 performance / lower priority; in the plan)
Change-ledger panel; AI cascade debounce/abort + surface `warn` severity; sheet-switch viewBox fix
(stop hardcoding 1600×900); tool-switch-with-blank-text-input snaps to Select; undo-button disabled
state read non-reactively; a11y labels. Delighters (Tier 2): before/after toggle (unused
`showDiff`/`toggleDiff`), one-click bid package, post-cascade plain-language summary.

---

## Guardrails (Mike's standing rules)
- Commit/push only when Mike asks. **Never push `main` by default** (this session's main pushes were an
  explicit one-time authorization). Revert the `auth.ts` dev-login before any commit. No customer
  `.dwg/.svg` in the repo (delete any staged QA file before committing).
- Never overwrite a deliverable — version/timestamp (this file is `-v5`).
- Develop from the LOCAL clone, never Google Drive. Port 3002.
- **Do better testing:** verify every fix LIVE on the real drawing (draw/select/delete/edit/stretch),
  not just `npm test`, before claiming done or deploying.
- Fable operating model: Opus contracts + writes the RED harness + verifies; Fable builds the durable
  algorithm. Mike loosened the ROI bar this session (limited window, credits) — farm harnessable durable
  units to Fable liberally.
- BLUF, no em-dashes, decide-and-act on reversible work, one question at a time.

---

## Key locations
- **App (work dir):** `/Users/mike/dev/EnergyLinkFlex` (branch `wip/dwg-ai-stretch-checkpoint`).
- **Phase A code:** `src/lib/dwg/hatch.ts` (dampenHatch, A1), `src/components/editor/{svg-drawing-canvas.tsx (A1-wire/A2/A3/A3-scope/A5), markup-overlay.tsx (A2/A4), nl-bar.tsx (A6)}`.
- **Plan (Phase B + UX):** `docs/superpowers/plans/2026-07-07-fixes-and-ux-plan.md`.
- **Investigation findings:** the `elf-investigate-solutionize` workflow output (5 agents) — journal under
  `…/subagents/workflows/wf_f2e09825-2f8/` (if the session dir persists); the key findings are folded into
  the plan doc + `project_status` memory.
- **Tests:** `src/lib/dwg/__tests__/` + `src/stores/__tests__/`; `npm test` = 139.
- **Handoffs:** this = `docs/HANDOFF-2026-07-07-v5.md`; hotstart = `docs/HOTSTART-2026-07-07-v5.md`.
- **Lab (real DWG/SVG, probes):** `~/dev/elf-lab` (incl. `24081-CS1-0001_Sheet_2.svg` for hatch analysis).
