Resuming EnergyLink FLEX. First read the cumulative handoff at /Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-08-v6.md and the project memory (MEMORY.md auto-loads; start with project_status.md — the SESSION 8 block is freshest — then fable_operating_model.md). Also read the plan doc docs/superpowers/plans/2026-07-07-fixes-and-ux-plan.md (Tier framing for what's left). Trust them, don't re-derive.

One-line context: session 8 SHIPPED TO PROD (energylinkflex.vercel.app @ commit 4e9327e = main = wip = origin): BLOB v2 hatch (small circle/ellipse/path now grayed too), B1 Undo/Redo (button + Cmd/Ctrl+Z), B2 persist dimension edits (new dwg_dim_edits jsonb column + /api/dwg/dimensions route; edits survive reload), B4 save indicator (Saving…/Saved), B6 multi-sheet blank fix (setSheets activates sheet 0), and left-justified sidebar component names — all live-verified + smoke-checked on prod. vitest 156/156, tsc 0, next build green. The "black blobs" question is SETTLED (residual dark = real Model_Space equipment, NOT removable) — do NOT re-investigate.

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint = main = prod @ 4e9327e, tree clean, in sync with origin). NEVER the Google Drive copy. Dev server = port 3002.

Confirm git state (git log --oneline -1 => 4e9327e; git status clean; 0 ahead/behind origin/main) and npm test green (156/156), then DO THESE IN ORDER (no need to ask first — I already chose this sequence):

B3 — Wire Export → PDF + PNG (biggest; the Export button is still DEAD, no onClick). Serialize the LIVE drawing (current stretches + red markups) → PDF + PNG, PDF stamped with the title block from dwgMetadata (drawing #, customer, scale). The rendered drawing is the big clipped <svg> in svg-drawing-canvas.tsx; markups are a separate overlay <svg> inside the transform wrapper — compose them. PNG via canvas.toBlob; PDF via a lib — CHECK package.json first (pdfjs-dist is present but read-side). Live-verify (download opens, dims + markups present, title block correct) before deploying.

B5 — Markups follow a stretch (Fable candidate). Overlay viewBox is frozen at load; a stretch grows the drawing viewBox so a red arrow drifts off target (correctness risk on a bid). Re-sync the overlay viewBox after applyAllStretches, or remap markup coords. Harnessable (pre/post viewBox → markup stays on target) — farm the coord-remap to Fable under an Opus RED harness (keep the operating-model discipline).

Then reassess Tier 1 (change-ledger panel, AI-cascade debounce/abort + surface warn severity, sheet-switch viewBox fix) and Tier 2 delighters (before/after toggle, one-click bid package, plain-language summary) from the plan doc.

How to verify LIVE (critical — do better testing before claiming done): re-add the DEV-ONLY auth.ts login shortcut (revert before any commit), set launch.json to port 3002 (revert to 3000 before commit — tracked file), preview_start "dev" on 3002, log in energylink/Linder, open project 215a08eb, and actually exercise the change (export/download; draw a markup then stretch and confirm it follows). To create dim/undo history in QA, select a component and click a quick-adjust BUTTON (+1' etc.) — text-input typing is flaky. DO NOT verify on a Vercel preview deployment (preview env lacks DB/auth vars so login fails); use local dev or prod only. If a "useEffect deps changed size" console error appears after editing a deps array, it's Turbopack HMR staleness — restart the dev server to clear it. curl isn't on PATH; smoke-check prod with node -e "fetch(...)".

Guardrails: commit/push only when I ask; NEVER push main by default (session 8's deploy was an explicit one-time OK). Revert auth.ts dev-login + launch.json port before any commit. No customer .dwg/.svg in the repo. Never overwrite a deliverable (version it). Fable = build harnessable durable algorithms (ROI bar loosened; farm liberally). BLUF, no em-dashes, decide-and-act on reversible work, one question at a time.

Deploy path when a chunk is verified (ask me before the FIRST prod deploy of the session): revert auth.ts + launch.json → next build (gate) → commit on wip → git push origin wip/dwg-ai-stretch-checkpoint → FF main (git push origin wip/dwg-ai-stretch-checkpoint:main) → vercel --prod --yes → smoke-check prod boots (node fetch: /login 200, /api/auth/session 200, / & /editor 307). The dwg_dim_edits column is already live in prod DB; no migration needed at deploy time.
</content>
