Resuming EnergyLink FLEX. First read the cumulative handoff at
/Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-06-v2.md and the project memory (MEMORY.md
auto-loads; start with project_status.md top "LIVE RUN VERIFIED" block, then
fable_operating_model.md and package_b_parked.md). Trust them — don't re-derive.

One-line context: over three sessions we fixed the DATA PIPELINE (never the model) and shipped
the whole engine — parse (crash fixed) → dim extraction → AI pre-scan → multi-dim + NESTED-ZONE
parametric stretch → auto dim re-valuing → per-view scoping with a safety net → clean render.
Session 3 ran a Fable 5 experiment (contract & verify): Fable built the nested-zone segment-map
engine (Package A, committed, verified) while Opus owned the spec + harness; Package B
(detached-detail scoping) is PARKED with findings. Then I UNBLOCKED and verified the LIVE RUN
end-to-end on Opus 4.8 in the browser (upload → AI pre-scan 9 sections → AI cascade → stretch →
clean render), and live QA caught + fixed a real crash. All committed; vitest 91/91, tsc, lint,
and a real-drawing golden green.

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint,
9 commits ahead of origin/wip, NOT pushed) — NEVER the Google Drive copy (FUSE won't boot the
dev server). Dev server = port 3002. Lab at ~/dev/elf-lab holds the real DWG/SVG, linkedom, the
golden runner (register.mjs), and the Package-B probes.

AI creds: direct-Anthropic now (Vercel Gateway needs a card the mikebetz team lacks). THINKING_MODEL
uses ANTHROPIC_API_KEY (in gitignored .env.local) with an explicit /v1 baseURL — required because
Claude Code's shell sets a bare ANTHROPIC_BASE_URL that 404s the default provider. Falls back to
the gateway when no key (prod unchanged).

Guardrails: commit/push ONLY when I ask; never push main (prod deploy). The auth.ts dev-login
shortcut is reverted (re-add it to log in locally as "energylink" on 3002, then revert before any
commit). No customer .dwg/.svg in the repo. Never overwrite a deliverable (version it). BLUF, no
em-dashes, decide-and-act on reversible work, one question at a time.

Confirm git state (git log --oneline -3 => 2d85066 / e9be754 / 4b8acfa) and npm test green (91/91),
then ask me which direction I want before writing code. Likely candidates:
  1. Push the branch / open a PR (only if I say so).
  2. Finish the browser QA checklist live (docs/superpowers/plans/2026-07-06-spatial-scoping-QA.md):
     width/duct-length stretch, undo/redo, explicit nested total+component edit. Needs the app on
     3002 + the dev-login re-added; test project 215a08eb-403d-41c7-b0f9-0fd6cceecac2 is in Neon.
  3. Make PRODUCTION AI work (add the Vercel Gateway card, OR set ANTHROPIC_API_KEY in prod env).
  4. Package B (detached-detail scoping) via the semantic/morphological reframe — see
     package_b_parked memory; real golden = Sheet_3 pumps stay round.
  5. Mechanical parser known-defects (text escaping, nozzle labels, GA-vs-P&ID sheet-type detection).
