# Hotstart prompt — EnergyLink FLEX, session 10 (paste into a new session)

> Copy everything in the fenced block below into a fresh session.

```
Resuming EnergyLink FLEX. First read the cumulative handoff at /Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-08-v7.md and the project memory (MEMORY.md auto-loads; start with project_status.md — the SESSION 9 block is freshest — then fable_operating_model.md). Also skim the plan doc docs/superpowers/plans/2026-07-07-fixes-and-ux-plan.md (Tier framing for what's left). Trust them, don't re-derive.

One-line context: session 9 SHIPPED TO PROD (energylinkflex.vercel.app @ commit 025d692 = main = wip = origin): B3 Export→PDF+PNG, B5 markups-follow-a-stretch (Fable-built coordinate remap + viewBox sync), all of Tier 1 (change-ledger panel, AI-cascade debounce/abort + "checking" pill + surfaced warn severity, sheet-switch viewBox), a Tier-2 one-click bid package (2-page branded PDF), and a hardening pass (adversarial multi-agent review = 13 findings, 10 fixed incl. 3 HIGH, 3 deferred). All live-verified + gates green (vitest 209/209, tsc 0, eslint 0, next build). New dep: jsPDF. The "black blobs" question is SETTLED (residual dark = real Model_Space equipment) — do NOT re-investigate.

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint = main = prod @ 025d692, tree clean, in sync with origin). NEVER the Google Drive copy. Dev server = port 3002. Bash cwd resets to the Google Drive path between calls — always `cd /Users/mike/dev/EnergyLinkFlex` first (or use absolute paths).

Confirm git state (git log --oneline -1 => 025d692; git status clean; 0 ahead/behind origin/main) and npm test green (209/209), then ask me what to tackle. The likely queue (from the handoff "OPEN WORK"):
- Tier 2: before/after toggle (store has unused showDiff/toggleDiff — show unstretched vs stretched); post-cascade plain-language summary (runtime-AI, NOT a Fable candidate).
- #8 (deferred): multi-sheet edit-tracking — switching sheets doesn't reset the change ledger / undo (they're global). Proper fix = per-sheet tracking; the naive reset would WIPE persisted dwg_dim_edits, so design it carefully. Single-sheet is unaffected.
- Tier 1 leftovers: tool-switch-with-blank-text-input snaps to Select; a11y labels on dim inputs.

How to verify LIVE (do this properly before claiming done): re-add the DEV-ONLY auth.ts login shortcut (top of Credentials authorize: if NODE_ENV!=="production" && DEV_LOGIN_EMAIL && email===DEV_LOGIN_EMAIL, look up the user_profiles row and return it — revert before any commit), set launch.json to port 3002 (revert to 3000 before commit — tracked file), preview_start "dev", log in energylink/Linder, open project 215a08eb, and actually exercise the change. To make a deterministic stretch in QA, select a component and click the +1' quick-adjust BUTTON (quickAdjust is cascade-free; use 4000 Stack or SCR Duct — Silencer's dim has no stretch block). To exercise the AI cascade (pending pill / warn banner), use an INLINE dim edit (click a dim in the SVG → editor → ✓). Selecting a component swaps the sidebar to a detail panel — click ✕ to get the list back. DO NOT verify on a Vercel preview deployment (preview env lacks DB/auth vars). curl isn't on PATH — smoke prod with node -e "fetch(...)". If a "useEffect deps changed size" console error appears after editing a deps array, it's Turbopack HMR staleness — restart the dev server.

Fable: an 8h window may still be open — Mike will say. Use the operating-model discipline (Opus contracts + writes the RED harness + verifies; Fable builds durable deterministic code, no runtime LLM; anti-cheat = Fable may not touch tests). Farm harnessable durable algorithms; keep runtime-AI features in Opus.

Guardrails: commit/push only when I ask; NEVER push main by default (sessions 7/8/9 deploys were each an explicit one-time OK). Revert auth.ts dev-login + launch.json port before any commit. No customer .dwg/.svg in the repo. Never overwrite a deliverable (version it). BLUF, no em-dashes, decide-and-act on reversible work, one question at a time.

Deploy path when a chunk is verified (ask me before the FIRST prod deploy of the session): revert auth.ts + launch.json → next build (gate) → commit on wip → git push origin wip/dwg-ai-stretch-checkpoint → FF main (git push origin wip/dwg-ai-stretch-checkpoint:main) → vercel --prod --yes → smoke-check prod boots (node fetch: /login 200, /api/auth/session 200, / & /editor 307). DB columns (dwg_sheet_type, dwg_markups, dwg_dim_edits) are all already live in prod; no migration needed.
```
