Resuming EnergyLink FLEX. First read the cumulative handoff at
/Users/mike/dev/EnergyLinkFlex/docs/HANDOFF-2026-07-07-v5.md and the project memory (MEMORY.md
auto-loads; start with project_status.md — the SESSION 7 block is freshest — then package_b_parked.md,
fable_operating_model.md). Also read the plan doc docs/superpowers/plans/2026-07-07-fixes-and-ux-plan.md.
Trust them, don't re-derive.

One-line context: session 7 SHIPPED TO PROD (energylinkflex.vercel.app @ commit 65e5fcd = main = wip):
the P&ID handling + red markups + buttons-only zoom feature, a white-overlay fix, and Phase A hotfixes
(A1 dampenHatch blobs, A2 markup select/delete, A3 component-overlap cycle, A3-scope .elf-dwg CSS,
A4 return-to-pan, A5 dim-popup dismiss, A6 "Ask FLEX AI" NL bar) — all live-verified. Prod DB migrations
(dwg_sheet_type, dwg_markups) already run on the shared Neon (== prod DATABASE_URL). vitest 139/139, tsc 0.

Work from the LOCAL clone /Users/mike/dev/EnergyLinkFlex (branch wip/dwg-ai-stretch-checkpoint, HEAD
65e5fcd, tree clean, in sync with origin/main). NEVER the Google Drive copy. Dev server = port 3002.

Confirm git state (git log --oneline -1 => 65e5fcd; git status clean; 0 ahead of origin/main) and npm
test green (139/139), then DO THESE IN ORDER (no need to ask first — Mike already chose this sequence):

1) BLOB v2 (small, first). A1's dampenHatch (src/lib/dwg/hatch.ts) is <line>-only and misses dense
   <path> and <circle>/<ellipse> equipment clusters, so a few small residual black blobs remain on prod.
   Extend dampenHatch (same ingestion-string pass; keep the Opus-RED-harness → Fable/verify discipline)
   to also gray short <path> subsegments and small <circle r>/<ellipse rx ry>. Add RED cases to
   __tests__/hatch.test.ts first. VERIFY LIVE on local dev (residual blobs visibly gone on Sheet_2
   equipment tops — energylink/Linder, project 215a08eb) before deploying.

2) PHASE B — restore the dead basics (the real trust-killers; full detail + Kano/Chisnell framing in the
   plan doc). Ordered: B1 wire Undo/Redo button + Cmd/Ctrl+Z (store logic already exists); B2 persist
   dimension edits (mirror the markup debounce PATCH); B3 wire Export → PDF + PNG (Mike's choice), PDF
   stamped with the title block from dwgMetadata; B4 save-state indicator; B5 markups-follow-stretch
   (re-sync overlay viewBox after applyAllStretches — Fable candidate); B6 multi-sheet blank fix
   (setSheets must setActiveSheet(0)/set svgUrl). Live-verify each before deploying.

How to verify LIVE (critical — do better testing before claiming done): re-add the DEV-ONLY auth.ts
login shortcut (revert before any commit), preview_start "dev" on 3002, log in energylink/Linder,
open the project, and actually exercise the change (draw/select/delete/edit-dimension/stretch/export).
DO NOT verify on a Vercel preview deployment — preview env lacks the DB/auth vars so login fails; use
local dev or prod only.

Guardrails: commit/push only when Mike asks; the session-7 main pushes were an explicit one-time OK, so
by default DON'T push main without asking. Revert auth.ts dev-login before any commit. No customer
.dwg/.svg in the repo. Never overwrite a deliverable (version it). Fable = build harnessable durable
algorithms (Mike loosened the ROI bar; farm liberally). BLUF, no em-dashes, decide-and-act on reversible
work, one question at a time.

Deploy path when a chunk is verified: revert auth.ts → git push origin wip → FF main
(git push origin wip/dwg-ai-stretch-checkpoint:main) → vercel --prod --yes → smoke-check prod boots.
Ask Mike before the FIRST prod deploy of the new session (main-push default guardrail).
